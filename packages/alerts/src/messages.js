import { defineMessages } from "react-intl";
export default defineMessages({
  generalError: {
    id: "alert.error.general",
    defaultMessage:
      "Oops, something went wrong. Please try again later or contact support."
  }
});
