name: CI

on: 
  pull_request:
    branches: 
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Build & deploy
      run: |
        setup-npm
        lerna bootstrap
        yarn run build:docs
        yarn run docs:deploy
