{
  "name": "{{ kitname }}/{{kebab name}}",
  "version": "1.0.0",
  "description": "{{ description }}",
  "main": "dist/index.js",
  "module": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org"
  }
  "public": true,
  "author": "Happeo Oy",
  "license": "MIT",
  "dependencies": {
    "@happeouikit/colors": "^{{ happeo-colors-version }}"
  },
  "devDependencies": {
    "@happeo/docs-tools": "^{{ happeo-docs-version }}"
  },
  "peerDependencies": {
    "react": "{{ react-version }}",
    "styled-components": "{{ styled-components-version }}",
    "prop-types": "{{ prop-types-version }}"
  }
}
