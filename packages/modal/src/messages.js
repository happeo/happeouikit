import { defineMessages } from "react-intl";
export default defineMessages({
  ok: {
    id: "confirm.modal.action.ok",
    defaultMessage: "Ok"
  },
  cancel: {
    id: "confirm.modal.action.cancel",
    defaultMessage: "Cancel"
  }
});
